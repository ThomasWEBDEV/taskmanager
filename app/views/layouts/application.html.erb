<!DOCTYPE html>
<html lang="fr">
  <head>
    <title>TaskManager - Gestion moderne de projets</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Application moderne de gestion de projets et tâches">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom Styles -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Navigation moderne -->
    <nav class="navbar navbar-expand-lg navbar-custom">
      <div class="container">
        <%= link_to root_path, class: "navbar-brand navbar-brand-custom" do %>
          <i class="fas fa-tasks me-2"></i>
          TaskManager
        <% end %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <div class="navbar-nav ms-auto align-items-center">
            <% if user_signed_in? %>
              <!-- Informations utilisateur -->
              <span class="user-info">
                <i class="fas fa-user-circle me-1"></i>
                Bonjour <%= current_user.email.split('@').first.capitalize %>
              </span>

              <!-- Navigation principale -->
              <%= link_to projects_path, class: "nav-link nav-link-custom" do %>
                <i class="fas fa-folder me-1"></i>
                Mes Projets
              <% end %>

              <%= link_to tasks_path, class: "nav-link nav-link-custom" do %>
                <i class="fas fa-list-check me-1"></i>
                Mes Tâches
              <% end %>

              <!-- Déconnexion -->
              <%= link_to destroy_user_session_path,
                  data: { "turbo-method": :delete },
                  class: "btn btn-outline-danger btn-sm ms-3" do %>
                <i class="fas fa-sign-out-alt me-1"></i>
                Déconnexion
              <% end %>
            <% else %>
              <!-- Liens d'authentification -->
              <%= link_to new_user_session_path, class: "btn btn-outline-primary btn-sm me-2" do %>
                <i class="fas fa-sign-in-alt me-1"></i>
                Connexion
              <% end %>

              <%= link_to new_user_registration_path, class: "btn btn-primary btn-sm" do %>
                <i class="fas fa-user-plus me-1"></i>
                Inscription
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Contenu principal -->
    <div class="container main-container">
      <!-- Notifications améliorées -->
      <% if notice %>
        <div class="alert alert-success alert-modern alert-dismissible fade show" role="alert">
          <i class="fas fa-check-circle me-2"></i>
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <% if alert %>
        <div class="alert alert-danger alert-modern alert-dismissible fade show" role="alert">
          <i class="fas fa-exclamation-triangle me-2"></i>
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <!-- Contenu de la page -->
      <%= yield %>
    </div>

    <!-- Footer moderne -->
    <footer class="footer-custom text-center">
      <div class="container">
        <p class="mb-0">
          <i class="fas fa-code me-2"></i>
          © 2025 TaskManager - Gestion moderne de projets et tâches
        </p>
      </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
